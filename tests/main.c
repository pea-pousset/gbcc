/* Multiline
  comment */
byte;
byte global_variable;

void main()
{
    byte foo = 6, toto, bar = 7; // Single line comment
    byte foobar = foo * bar;
    foobar; // Invoking a variable's name displays its value on screen
    return foobar;
}
